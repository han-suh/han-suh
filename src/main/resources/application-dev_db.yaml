spring:
  datasource:
    url: jdbc:mysql://localhost:3306/artu
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

server:
  port: 4775

# 로그 레벨 설정:
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace